# FR-10 이격거리 규정 판정 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 부지의 시군구 조례가 정한 이격거리 규정이 「신에너지 및 재생에너지 개발ㆍ이용ㆍ보급 촉진법」 제27조의3(2026-09-18 시행)에 비추어 실제로 적용되는지를 판정해 기존 판정표에 한 행으로 추가한다.

**Architecture:** 조례 수치는 사람이 확정한 CSV(`data/setback_table.csv`)에 두고, 판정은 네트워크를 호출하지 않는 순수 함수(`src/setback_check.py`)가 수행한다. 기존 `match_regulations`/`judge`가 `rule_table`을 DataFrame으로 주입받는 패턴을 그대로 따른다. 판정 결과는 어댑터 함수를 통해 기존 판정표 행 구조로 변환되어 CLI와 웹앱 양쪽에 동일하게 붙는다.

**Tech Stack:** Python 3.14, pandas, pytest (TDD). 신규 외부 의존성 없음.

**Spec:** [docs/20260916_설계_FR-10 이격거리 규정 판정 v1.0.md](20260916_설계_FR-10%20이격거리%20규정%20판정%20v1.0.md)

## Global Constraints

- **불확실하면 절대 "비저촉"으로 단정하지 않는다.** 확인 불가 구간은 `판정불가`로 반환한다. (기존 `judge.py` 원칙과 동일)
- 이번 사이클에서 `저촉` 상태는 반환하지 않는다. 반경 내 주택 실측이 범위 밖이므로 저촉을 확정할 근거가 없다.
- 판정 상태 문자열은 기존 체계를 그대로 쓴다: `비저촉` / `조건부` / `판정불가`.
- 시행일 상수는 `STATUTE_EFFECTIVE_DATE = date(2026, 9, 18)` 한 곳에만 둔다. 이 값은 법령 API 검증이 아니라 부칙 제1조("공포 후 6개월이 경과한 날", 공포 2026-03-17)에 근거한 사람의 확정값이며, 코드 주석에 그 사실을 남긴다.
- `setback_table.csv`에서 `verified_by`가 비어 있는 행은 **판정에 사용하지 않는다**(자동 수집 초안이 사람 검토 없이 판정에 반영되는 것을 구조적으로 차단).
- 새 폴더를 만들면 그 안에 `README.md`를 함께 만든다 (프로젝트 CLAUDE.md 규칙).
- 커밋 메시지는 한국어로, 기존 저장소 관례를 따른다.

---

## File Structure

| 파일 | 상태 | 책임 |
|---|---|---|
| `.env` | 수정 | 법령 API 정식 OC 키 적용 (`OC=test` 데모 의존 제거) |
| `src/law_lookup.py` | 수정 | 가지번호 조문(제27조의3) 조회 지원 추가 |
| `tests/fixtures/sample_law.xml` | 수정 | 가지번호 조문 블록 추가 |
| `tests/test_law_lookup.py` | 수정 | 가지번호 조회 테스트 추가 |
| `data/setback_table.csv` | 신규 | 시군구별 조례 이격거리 (사람 확정 데이터) |
| `scripts/collect_setback_draft.py` | 신규 | 전남 22개 시군구 조례 자동 수집 → 초안 CSV |
| `src/setback_check.py` | 신규 | 판정 엔진(순수 함수) + 판정표 행 어댑터 |
| `tests/test_setback_check.py` | 신규 | 판정 규칙 R1~R6 테스트 |
| `src/query_site_data.py` | 수정 | GIS 결과의 `sgg_cd`를 파이프라인 밖으로 전달 |
| `tests/test_query_site_data.py` | 수정 | `sgg_cd` 전달 테스트 |
| `src/judge.py` | 수정 | CLI 실행 시 이격거리 행 추가 |
| `api/site_judge.py` | 수정 | 웹앱 응답에 이격거리 행 추가 + 입력 파라미터 확장 |
| `public/index.html` | 수정 | 면제사유 체크박스 · 허가신청 예정일 입력 |
| `PRD.md`, `HANDOVER.md` | 수정 | FR-10 반영 |

---

### Task 0: 법령 API 정식 키 적용

지금까지 `OC=test`(비등록 데모 접근)로 법령을 조회해 왔다(HANDOVER §4에 알려진 한계로 기록됨). 이 기능은 법령 조회 정확도에 직접 의존하므로, 정식 키부터 적용한다. 키는 상위 폴더 `API 발급키.env`의 `LAW_API_KEY`에 있으며 동작을 이미 확인했다.

**Files:**
- Modify: `.env` (git 추적 대상 아님)
- Modify: `HANDOVER.md`

**Interfaces:**
- Consumes: 없음
- Produces: 환경변수 `LAW_GO_KR_OC` — `src/law_lookup.py`의 `DEFAULT_OC`가 이미 이 이름을 읽는다(코드 변경 불필요)

- [ ] **Step 1: .env에 키 추가**

Run:
```bash
LAWKEY=$(grep -E "^LAW_API_KEY=" "../API 발급키.env" | cut -d= -f2- | tr -d '\r\n" ')
printf "LAW_GO_KR_OC=%s\n" "$LAWKEY" >> .env
grep -c "LAW_GO_KR_OC" .env
```
Expected: `1`

> `.env`는 `.gitignore`에 있어 커밋되지 않는다. 키를 코드나 문서에 직접 적지 않는다.

- [ ] **Step 2: 정식 키로 조회되는지 확인**

Run:
```bash
python -c "
import sys; sys.path.insert(0,'src')
from law_lookup import DEFAULT_OC, lookup_article_text as L
print('OC is test?', DEFAULT_OC == 'test')
r = L('국토의 계획 및 이용에 관한 법률', '제76조')
print('status:', r['status'], '| text len:', len(r.get('text','')))
"
```
Expected: `OC is test? False`, `status: ok`, 본문 길이 1000자 이상

- [ ] **Step 3: 전체 테스트**

Run: `python -m pytest -q`
Expected: 전부 PASS (테스트는 픽스처 기반이라 키와 무관하지만, 회귀 확인)

- [ ] **Step 4: HANDOVER의 알려진 한계 갱신**

`HANDOVER.md`의 `## 5. 알려진 한계` 절에서 law.go.kr `OC=test` 관련 항목을 찾아 아래로 교체한다.

```markdown
- ~~국가법령정보센터(law.go.kr) Open API를 `OC=test` 데모 접근으로 사용~~ → **해소됨(2026-09-17)**: 정식 OC 키를 발급받아 `.env`의 `LAW_GO_KR_OC`에 적용. 다만 시행일자별 조회(`efYd` 파라미터)는 정식 키로도 동작하지 않는 것을 확인했다 — 특정 시행일 시점의 조문 버전을 가져오는 용도로는 쓸 수 없다
```

`## 4. 장애 대응` 표의 `check_law_updates.py에서 lookup_failed 다수 발생` 행에서 "OC 등록 후 .env에 추가" 부분도 "이미 적용됨(2026-09-17)"으로 갱신한다.

- [ ] **Step 5: 커밋**

```bash
git add HANDOVER.md
git commit -m "docs: 법령 API 정식 OC 키 적용 반영

OC=test 데모 접근 의존을 해소하고 알려진 한계에서 제거.
efYd(시행일자별 조회)는 정식 키로도 동작하지 않음을 함께 기록."
```

---

### Task 1: law_lookup 가지번호 조문 조회

제27조의3을 요청하면 제27조가 반환되는 버그를 고친다. 이 법에만 가지번호 조문이 31개이고, 이 기능의 근거 조문이 바로 제27조의3이므로 선행 작업이다.

**Files:**
- Modify: `src/law_lookup.py:19` (패턴), `src/law_lookup.py:23-26` (추출), `src/law_lookup.py:52-71` (파싱), `src/law_lookup.py:255-266` (조회)
- Modify: `tests/fixtures/sample_law.xml`
- Test: `tests/test_law_lookup.py`

**Interfaces:**
- Consumes: 없음 (첫 태스크)
- Produces:
  - `extract_article_parts(law_article: str) -> tuple[str, str | None] | None` — `"제27조의3"` → `("27", "3")`, `"제76조"` → `("76", None)`, `"별표20"` → `None`
  - `parse_article(xml_text: str, article_no: str, branch_no: str | None = None) -> dict | None` — 반환 dict는 기존과 동일하게 `{"article_title", "text", "effective_date"}`
  - `extract_article_number`, `lookup_article_text`의 시그니처는 그대로 유지

- [ ] **Step 1: 픽스처에 가지번호 조문 추가**

`tests/fixtures/sample_law.xml`에서 `제76조` 블록(`</조문단위>`로 끝나는 부분, 43번째 줄 근처) **바로 뒤**에 아래 블록을 삽입한다. 파일 끝의 `</조문>`, `</법령>`은 그대로 둔다.

```xml
<조문단위 조문키="0076002">
<조문번호>76</조문번호>
<조문가지번호>2</조문가지번호>
<조문여부>조문</조문여부>
<조문제목><![CDATA[가지번호 조문 테스트용 제목]]></조문제목>
<조문시행일자>20260701</조문시행일자>
<조문변경여부>N</조문변경여부>
<조문내용><![CDATA[제76조의2(가지번호 조문 테스트용 제목)]]></조문내용>
<항>
<항번호><![CDATA[①]]></항번호>
<항내용><![CDATA[① 이것은 제76조의2의 내용이며 제76조와 구별되어야 한다.]]></항내용>
</항>
</조문단위>
```

- [ ] **Step 2: 실패하는 테스트 작성**

`tests/test_law_lookup.py` 끝에 추가한다.

```python
def test_parse_article_with_branch_number_returns_branch_article():
    result = parse_article(FIXTURE, "76", branch_no="2")

    assert result is not None
    assert result["article_title"] == "가지번호 조문 테스트용 제목"
    assert "제76조의2의 내용" in result["text"]


def test_parse_article_without_branch_number_skips_branch_articles():
    result = parse_article(FIXTURE, "76")

    assert result["article_title"] == "용도지역 및 용도지구에서의 건축물의 건축 제한 등"
    assert "제76조의2의 내용" not in result["text"]


def test_extract_article_parts_splits_branch_number():
    assert extract_article_parts("제27조의3") == ("27", "3")
    assert extract_article_parts("제76조") == ("76", None)
    assert extract_article_parts("별표20") is None
```

같은 파일 상단 import 문에 `extract_article_parts`를 추가한다.

```python
from law_lookup import (
    parse_article, extract_article_number, extract_article_parts,
    parse_appendix, extract_appendix_number,
)
```

> 기존 import 문은 위 형태와 다를 수 있다. 기존 목록에 `extract_article_parts`만 추가하면 된다.

- [ ] **Step 3: 테스트 실패 확인**

Run: `python -m pytest tests/test_law_lookup.py -v -k "branch or parts"`
Expected: FAIL — `ImportError: cannot import name 'extract_article_parts'`

- [ ] **Step 4: 최소 구현**

`src/law_lookup.py`의 19번째 줄 패턴을 교체한다.

```python
_ARTICLE_NO_PATTERN = re.compile(r"제(\d+)조(?:의\s*(\d+))?")
```

`extract_article_number` 바로 아래에 함수를 추가한다.

```python
def extract_article_parts(law_article: str) -> tuple[str, str | None] | None:
    """'제27조의3' -> ('27', '3'), '제76조' -> ('76', None), '별표20' -> None.

    law.go.kr XML은 가지번호 조문을 <조문번호>27</조문번호><조문가지번호>3</조문가지번호>로
    표현한다. 조문번호만 비교하면 제27조와 제27조의2·의3이 전부 같은 조문으로 취급된다.
    """
    match = _ARTICLE_NO_PATTERN.search(law_article or "")
    if match is None:
        return None
    return match.group(1), match.group(2)
```

`parse_article`의 시그니처와 비교문을 바꾼다.

```python
def parse_article(xml_text: str, article_no: str, branch_no: str | None = None) -> dict | None:
    """법령 전문 XML에서 article_no(+가지번호)에 해당하는 조문만 추출.
    각 항에 중첩된 호ㆍ목(세부 목록)까지 포함해 누락 없이 가져온다."""
    root = ET.fromstring(xml_text)
    effective_date = root.findtext(".//기본정보/시행일자")

    for unit in root.findall(".//조문/조문단위"):
        title = unit.findtext("조문제목")
        if title is None:
            continue  # 조문제목이 없으면 장/절 헤더 - 실제 조문이 아님
        if unit.findtext("조문번호") != article_no:
            continue
        if (unit.findtext("조문가지번호") or None) != branch_no:
            continue

        paragraphs = [_paragraph_full_text(p) for p in unit.findall("항")]
        paragraphs = [p for p in paragraphs if p]
        text = "\n".join(paragraphs) if paragraphs else (unit.findtext("조문내용") or "")

        return {"article_title": title, "text": text, "effective_date": effective_date}

    return None
```

`lookup_article_text`의 조문 분기를 바꾼다(255번째 줄 근처).

```python
    parts = extract_article_parts(law_article)
    if parts is None:
        return {"status": "no_data", "reason": f"조문번호를 해석할 수 없음: '{law_article}'"}

    article_no, branch_no = parts
    label = f"제{article_no}조" + (f"의{branch_no}" if branch_no else "")
    return _lookup(law_name, oc, lambda xml: parse_article(xml, article_no, branch_no),
                    not_found_reason=f"{law_name}에서 {label}를 찾을 수 없음")
```

> `lookup_ordinance_text`(225번째 줄)는 건드리지 않는다. 자치법규는 조문번호 스키마가 달라(조번호×100 0-패딩) 가지번호 처리가 별개 문제이며 이번 범위 밖이다.

- [ ] **Step 5: 테스트 통과 확인**

Run: `python -m pytest tests/test_law_lookup.py -v`
Expected: PASS (신규 3개 포함 전부)

- [ ] **Step 6: 실제 법령으로 검증**

Run:
```bash
python -c "import sys; sys.path.insert(0,'src'); from law_lookup import lookup_article_text as L; r=L('신에너지 및 재생에너지 개발ㆍ이용ㆍ보급 촉진법','제27조의3'); print(r['status']); print(r.get('article_title'))"
```
Expected: `ok` 와 `재생에너지 발전설비의 이격거리` 출력. (수정 전에는 `보급사업`이 나왔다.)

- [ ] **Step 7: 전체 테스트 + 커밋**

Run: `python -m pytest -q`
Expected: 전부 PASS

```bash
git add src/law_lookup.py tests/test_law_lookup.py tests/fixtures/sample_law.xml
git commit -m "fix(law_lookup): 가지번호 조문(제27조의3) 조회 지원

조문번호만 비교해서 제27조의2·의3을 요청해도 제27조가 반환되던 문제 수정.
law.go.kr XML은 가지번호를 <조문가지번호>로 따로 두는데 이를 무시하고 있었다.
이 법 하나에만 가지번호 조문이 31개 있어 영향 범위가 작지 않다."
```

---

### Task 2: 기존 규칙표의 가지번호 조문 재검증

Task 1 이전에는 가지번호 조문이 전부 잘못 조회됐다. `rule_table.csv`에 그런 조문이 있으면 지금까지 **틀린 조문을 근거로 제시해 온 것**이므로 확인이 필요하다.

**Files:**
- Modify (필요 시): `data/rule_table.csv`

**Interfaces:**
- Consumes: Task 1의 `lookup_article_text`
- Produces: 없음 (데이터 정합성 확인)

- [ ] **Step 1: 가지번호 조문을 쓰는 규칙 식별**

Run:
```bash
python -c "
import csv
rows = list(csv.DictReader(open('data/rule_table.csv', encoding='utf-8')))
hits = [(r['rule_id'], r['law_name'], r['law_article']) for r in rows if '의' in r['law_article']]
print('가지번호 의심 행:', hits if hits else '없음')
"
```

- [ ] **Step 2: 의심 행이 없으면 이 태스크 종료**

출력이 `없음`이면 Step 3~4를 건너뛰고 Task 3으로 넘어간다. 커밋할 것도 없다.

- [ ] **Step 3: 의심 행이 있으면 재조회해 비교**

각 행에 대해 실행한다(`<법령명>`, `<조문>`을 Step 1 출력값으로 치환).

```bash
python -c "
import sys; sys.path.insert(0,'src')
from law_lookup import lookup_article_text as L
r = L('<법령명>', '<조문>')
print(r['status'], '|', r.get('article_title'))
print(r.get('text','')[:300])
"
```

기존 `law_excerpt`와 내용이 다르면 **사람이 조문을 읽고 확인한 뒤** `law_excerpt`를 교체한다. 자동으로 덮어쓰지 않는다(규칙표는 사람 확정 데이터).

- [ ] **Step 4: 변경이 있었다면 커밋**

```bash
git add data/rule_table.csv
git commit -m "fix(rule_table): 가지번호 조문 인용 정정

law_lookup의 가지번호 버그로 잘못된 조문이 인용돼 있던 항목을
정확한 조문으로 교체(사람 확인 후 반영)."
```

---

### Task 3: setback_table.csv 스키마 확정 + 수집 스크립트

판정 엔진이 읽을 데이터의 **모양을 먼저 고정**한다. 22개 시군구 조례 수집은 시간이 걸리므로, 스키마와 스크립트를 만들고 초안까지 생성하되 판정 엔진(Task 4)은 이 파일에 의존하지 않고 테스트용 DataFrame으로 개발한다.

**Files:**
- Create: `data/setback_table.csv`
- Create: `scripts/collect_setback_draft.py`
- Modify: `scripts/README.md`

**Interfaces:**
- Consumes: Task 1의 `lookup_ordinance_text`
- Produces: CSV 컬럼 순서 — `sgg_cd, sgg_nm, project_type, target, distance_m, min_house_count, ordinance_name, article, law_excerpt, gosi_date, source_url, verified_by, verified_at`

- [ ] **Step 1: 빈 스키마 파일 생성**

`data/setback_table.csv`를 아래 한 줄(헤더)만으로 만든다.

```csv
sgg_cd,sgg_nm,project_type,target,distance_m,min_house_count,ordinance_name,article,law_excerpt,gosi_date,source_url,verified_by,verified_at
```

- [ ] **Step 2: 수집 스크립트 작성**

`scripts/collect_setback_draft.py`:

```python
"""
[일회성 수집 스크립트] 전남 22개 시군구 도시계획 조례에서 이격거리 조문 초안을 모은다.

결과는 data/setback_table_draft.csv 로 저장되며, verified_by 가 비어 있으므로
판정 엔진(src/setback_check.py)은 이 초안을 판정에 사용하지 않는다.
사람이 조문을 읽고 수치를 확정한 뒤 data/setback_table.csv 로 옮겨야 한다.

주의: 조례 명칭은 지자체마다 다르다("OO시 도시계획 조례", "OO군 도시계획 조례" 등).
      조회에 실패한 시군구는 실패 사유와 함께 행으로 남겨 누락을 눈에 보이게 한다.
"""
import csv
import os
import sys
import time

sys.path.insert(0, os.path.join(os.path.dirname(os.path.dirname(os.path.abspath(__file__))), "src"))

from law_lookup import lookup_ordinance_text  # noqa: E402

BASE_DIR = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
OUT_PATH = os.path.join(BASE_DIR, "data", "setback_table_draft.csv")

# zoning_jeonnam.geojson 과 동일한 시군구코드 체계
JEONNAM_SGG = {
    "12110": "목포시", "12130": "여수시", "12150": "순천시", "12170": "나주시", "12190": "광양시",
    "12710": "담양군", "12720": "곡성군", "12730": "구례군", "12740": "고흥군", "12750": "보성군",
    "12760": "화순군", "12770": "장흥군", "12780": "강진군", "12790": "해남군", "12800": "영암군",
    "12810": "무안군", "12820": "함평군", "12830": "영광군", "12840": "장성군", "12850": "완도군",
    "12860": "진도군", "12870": "신안군",
}

# 조례에서 이격거리 규정이 있을 법한 조문 후보. 지자체마다 조문 번호가 달라 여러 개를 시도한다.
CANDIDATE_ARTICLES = ["제27조", "제28조", "제29조", "제30조", "제31조", "제32조"]

COLUMNS = [
    "sgg_cd", "sgg_nm", "project_type", "target", "distance_m", "min_house_count",
    "ordinance_name", "article", "law_excerpt", "gosi_date", "source_url",
    "verified_by", "verified_at",
]


def collect_one(sgg_nm):
    """한 시군구의 조례에서 '이격거리'가 언급된 조문을 모두 찾아 돌려준다."""
    ordinance_name = f"{sgg_nm} 도시계획 조례"
    found = []
    for article in CANDIDATE_ARTICLES:
        result = lookup_ordinance_text(ordinance_name, article)
        if result.get("status") != "ok":
            continue
        text = result.get("text", "")
        if "이격" in text:
            found.append({
                "article": article,
                "law_excerpt": text,
                "source_url": result.get("source_url", ""),
            })
        time.sleep(0.3)  # 연속 호출 간 최소 대기
    return ordinance_name, found


def main():
    rows = []
    for sgg_cd, sgg_nm in sorted(JEONNAM_SGG.items()):
        ordinance_name, found = collect_one(sgg_nm)
        if not found:
            # 못 찾은 것도 행으로 남겨서 "확인 안 함"이 눈에 보이게 한다
            rows.append({
                "sgg_cd": sgg_cd, "sgg_nm": sgg_nm, "project_type": "태양광",
                "target": "", "distance_m": "", "min_house_count": "",
                "ordinance_name": ordinance_name, "article": "",
                "law_excerpt": "(자동수집 실패 - 조문번호 후보에서 '이격' 언급 없음)",
                "gosi_date": "", "source_url": "", "verified_by": "", "verified_at": "",
            })
            print(f"{sgg_nm}: 후보 조문에서 찾지 못함")
            continue
        for item in found:
            rows.append({
                "sgg_cd": sgg_cd, "sgg_nm": sgg_nm, "project_type": "태양광",
                "target": "", "distance_m": "", "min_house_count": "",
                "ordinance_name": ordinance_name, "article": item["article"],
                "law_excerpt": item["law_excerpt"], "gosi_date": "",
                "source_url": item["source_url"], "verified_by": "", "verified_at": "",
            })
        print(f"{sgg_nm}: {len(found)}건 수집")

    with open(OUT_PATH, "w", encoding="utf-8-sig", newline="") as f:
        writer = csv.DictWriter(f, fieldnames=COLUMNS)
        writer.writeheader()
        writer.writerows(rows)

    print(f"\n초안 저장: {OUT_PATH} ({len(rows)}행)")
    print("verified_by 가 비어 있어 판정에는 사용되지 않는다. 사람이 확정 후 setback_table.csv 로 옮길 것.")


if __name__ == "__main__":
    main()
```

- [ ] **Step 3: 수집 실행**

Run: `python scripts/collect_setback_draft.py`
Expected: 22개 시군구에 대해 수집 결과 또는 실패 메시지가 출력되고 `data/setback_table_draft.csv` 생성.

> 자치법규 API가 조례를 못 찾는 시군구가 나올 수 있다. 정상이다. 그런 시군구는 판정 시 R2(판정불가)로 처리되므로 잘못된 판정이 나가지 않는다.

- [ ] **Step 4: scripts/README.md 갱신**

`scripts/README.md`의 "폴더 내 파일" 목록에 항목을 추가한다.

```markdown
- `collect_setback_draft.py`: 전남 22개 시군구 도시계획 조례에서 이격거리 조문 초안을 수집해 `../data/setback_table_draft.csv`로 저장한다. 수집 결과는 `verified_by`가 비어 있는 **초안**이며, 사람이 조문을 읽고 수치를 확정해 `../data/setback_table.csv`로 옮겨야 판정에 반영된다.
```

- [ ] **Step 5: 커밋**

`setback_table_draft.csv`는 사람 확정 전 중간 산출물이므로 커밋하지 않는다. `.gitignore`에 추가한다.

```bash
echo "" >> .gitignore
echo "# 사람 확정 전 조례 수집 초안 (setback_table.csv 로 옮긴 뒤 버림)" >> .gitignore
echo "data/setback_table_draft.csv" >> .gitignore
git add data/setback_table.csv scripts/collect_setback_draft.py scripts/README.md .gitignore
git commit -m "feat(setback): 조례 이격거리 표 스키마 + 자동 수집 스크립트

전남 22개 시군구 조례에서 이격거리 조문을 긁어 초안 CSV를 만든다.
초안은 verified_by 가 비어 있어 판정 엔진이 사용하지 않으며,
사람이 조문을 읽고 수치를 확정해야 setback_table.csv 에 반영된다."
```

---

### Task 4: 판정 엔진 `setback_check.py`

설계서 §6-2의 R1~R6 규칙을 순수 함수로 구현한다. 네트워크·파일 접근 없이 DataFrame을 주입받으므로 테스트가 빠르고 결정적이다.

**Files:**
- Create: `src/setback_check.py`
- Test: `tests/test_setback_check.py`

**Interfaces:**
- Consumes: 없음 (순수 함수)
- Produces:
  - `STATUTE_EFFECTIVE_DATE: datetime.date`
  - `check_setback(sgg_cd: str, project_type: str, apply_date: date, exemptions: list[str], zone_flags: dict | None, setback_table: pd.DataFrame) -> dict`
    반환: `{"status", "rule", "reason", "ordinance", "statute"}`
  - `to_judgment_row(result: dict) -> dict`
    반환: `{"raw_text", "rule_id", "status", "law_excerpt", "source_url", "note"}` — 기존 `judge.py` 출력 행과 동일한 키 구조

- [ ] **Step 1: 실패하는 테스트 작성**

`tests/test_setback_check.py`:

```python
import sys
from datetime import date
from pathlib import Path

import pandas as pd

sys.path.insert(0, str(Path(__file__).resolve().parent.parent / "src"))

from setback_check import STATUTE_EFFECTIVE_DATE, check_setback, to_judgment_row

COLUMNS = ["sgg_cd", "sgg_nm", "project_type", "target", "distance_m", "min_house_count",
           "ordinance_name", "article", "law_excerpt", "gosi_date", "source_url",
           "verified_by", "verified_at"]


def _table(rows):
    return pd.DataFrame(rows, columns=COLUMNS).fillna("")


CONFIRMED_MOKPO = {
    "sgg_cd": "12110", "sgg_nm": "목포시", "project_type": "태양광", "target": "주택",
    "distance_m": "100", "min_house_count": "10",
    "ordinance_name": "목포시 도시계획 조례", "article": "제27조",
    "law_excerpt": "주택 10호 이상 밀집지역 경계로부터 100미터",
    "gosi_date": "20220315", "source_url": "https://www.law.go.kr/test",
    "verified_by": "서장훈", "verified_at": "2026-09-17",
}

BEFORE = date(2026, 9, 1)
AFTER = date(2026, 10, 1)


def test_roof_mounted_solar_is_exempt_by_statute():
    result = check_setback("12110", "태양광", AFTER, ["지붕형"], {}, _table([CONFIRMED_MOKPO]))

    assert result["status"] == "비저촉"
    assert result["rule"] == "R1"
    assert "제27조의3" in result["statute"]["article"]


def test_resident_participation_is_exempt_by_statute():
    result = check_setback("12110", "태양광", AFTER, ["주민참여형"], {}, _table([CONFIRMED_MOKPO]))

    assert result["status"] == "비저촉"
    assert result["rule"] == "R1"


def test_unverified_row_is_not_used_for_judgment():
    draft = dict(CONFIRMED_MOKPO, verified_by="")
    result = check_setback("12110", "태양광", AFTER, [], {}, _table([draft]))

    assert result["status"] == "판정불가"
    assert result["rule"] == "R2"


def test_missing_sgg_returns_undecidable():
    result = check_setback("12999", "태양광", AFTER, [], {}, _table([CONFIRMED_MOKPO]))

    assert result["status"] == "판정불가"
    assert result["rule"] == "R2"


def test_confirmed_no_ordinance_rule_is_not_violating():
    none_row = dict(CONFIRMED_MOKPO, distance_m="", min_house_count="",
                    law_excerpt="이격거리 규정 없음")
    result = check_setback("12110", "태양광", AFTER, [], {}, _table([none_row]))

    assert result["status"] == "비저촉"
    assert result["rule"] == "R3"


def test_application_before_statute_keeps_ordinance():
    result = check_setback("12110", "태양광", BEFORE, [], {}, _table([CONFIRMED_MOKPO]))

    assert result["status"] == "조건부"
    assert result["rule"] == "R4"
    assert result["ordinance"]["distance_m"] == 100


def test_protected_zone_allows_ordinance_after_statute():
    flags = {"역사문화환경보존지역": False, "생태경관보전지역": True}
    result = check_setback("12110", "태양광", AFTER, [], flags, _table([CONFIRMED_MOKPO]))

    assert result["status"] == "조건부"
    assert result["rule"] == "R5-a"


def test_ordinary_zone_after_statute_is_undecidable_due_to_missing_decree():
    flags = {"역사문화환경보존지역": False, "생태경관보전지역": False}
    result = check_setback("12110", "태양광", AFTER, [], flags, _table([CONFIRMED_MOKPO]))

    assert result["status"] == "판정불가"
    assert result["rule"] == "R5-b"
    assert "대통령령" in result["reason"]


def test_unknown_zone_flags_are_undecidable():
    result = check_setback("12110", "태양광", AFTER, [], None, _table([CONFIRMED_MOKPO]))

    assert result["status"] == "판정불가"
    assert result["rule"] == "R6"


def test_effective_date_is_the_statute_boundary():
    flags = {"역사문화환경보존지역": False, "생태경관보전지역": False}
    on_the_day = check_setback("12110", "태양광", STATUTE_EFFECTIVE_DATE, [], flags,
                                _table([CONFIRMED_MOKPO]))

    assert on_the_day["rule"] == "R5-b"


def test_judgment_row_matches_existing_table_shape():
    result = check_setback("12110", "태양광", BEFORE, [], {}, _table([CONFIRMED_MOKPO]))
    row = to_judgment_row(result)

    assert row["raw_text"] == "이격거리(조례)"
    assert row["status"] == "조건부"
    assert "목포시 도시계획 조례" in row["law_excerpt"]
    assert row["source_url"] == "https://www.law.go.kr/test"
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `python -m pytest tests/test_setback_check.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'setback_check'`

- [ ] **Step 3: 최소 구현**

`src/setback_check.py`:

```python
"""
[FR-10] 이격거리 규정 판정.

「신에너지 및 재생에너지 개발ㆍ이용ㆍ보급 촉진법」 제27조의3(2026-09-18 시행)에 따라
지자체 조례의 이격거리 적용이 원칙적으로 금지된다. 이 모듈은 부지의 시군구 조례가
실제로 적용되는지를 판정한다.

설계 근거: docs/20260916_설계_FR-10 이격거리 규정 판정 v1.0.md

핵심 제약(2026-09-16 확인): 법 제27조의3 제1항제3호·제2항이 위임한 대통령령에
이격거리 조문이 아직 없다. 따라서 "원칙적으로 적용 불가지만 예외에 해당하는지"를
확정할 수 없는 구간이 존재하며, 그 구간은 판정불가로 둔다(R5-b).
확인되지 않은 수치를 근거로 비저촉/저촉을 단정하지 않는다.
"""
from datetime import date

import pandas as pd

# 법 제27조의3 시행일.
# 부칙(법률 제21462호, 2026.3.17) 제1조 "공포 후 6개월이 경과한 날"에 근거한 값이다.
# law.go.kr 상세조회가 efYd 파라미터를 무시해(OC=test 제약) API로 실증하지는 못했고,
# 2026-09-17 사람 판단으로 확정했다. 값이 바뀌면 이 상수만 고치면 된다.
STATUTE_EFFECTIVE_DATE = date(2026, 9, 18)

STATUTE = {
    "name": "신에너지 및 재생에너지 개발ㆍ이용ㆍ보급 촉진법",
    "article": "제27조의3",
    "effective_date": STATUTE_EFFECTIVE_DATE.isoformat(),
}

# 법 제27조의3제3항 각 호 - 해당하면 조례 이격거리가 적용되지 않는다
STATUTORY_EXEMPTIONS = {
    "주민참여형": "제27조의3제3항제1호(주민 참여형 발전설비)",
    "지붕형": "제27조의3제3항제2호(지붕형 태양광발전설비)",
    "자가소비용": "제27조의3제3항제3호(자가소비용 태양광발전설비)",
}

# 법 제27조의3제1항 각 호 - 해당하면 지자체가 이격거리를 적용할 수 있다
PROTECTED_ZONE_FLAGS = {
    "역사문화환경보존지역": "제27조의3제1항제1호(역사문화환경 보존지역·보호구역)",
    "생태경관보전지역": "제27조의3제1항제2호(생태ㆍ경관보전지역)",
}


def _to_int(value):
    """CSV에서 읽은 값을 정수로. 빈 값이면 None."""
    text = str(value).strip()
    if not text:
        return None
    try:
        return int(float(text))
    except ValueError:
        return None


def _ordinance_payload(row):
    return {
        "name": str(row["ordinance_name"]).strip(),
        "article": str(row["article"]).strip(),
        "distance_m": _to_int(row["distance_m"]),
        "min_house_count": _to_int(row["min_house_count"]),
        "excerpt": str(row["law_excerpt"]).strip(),
        "source_url": str(row["source_url"]).strip(),
    }


def check_setback(sgg_cd, project_type, apply_date, exemptions, zone_flags, setback_table):
    """조례 이격거리가 실제로 적용되는지 판정한다.

    확실하지 않으면 판정불가를 돌려준다 - 이 함수는 어떤 경우에도 '저촉'을 반환하지 않는다.
    반경 내 주택 실측이 범위 밖이라 저촉을 확정할 근거가 없기 때문이다.
    """
    # R1. 법이 정한 적용 배제 대상 (조례 내용과 무관하게 확정)
    for name in exemptions or []:
        if name in STATUTORY_EXEMPTIONS:
            return {
                "status": "비저촉",
                "rule": "R1",
                "reason": f"법 {STATUTORY_EXEMPTIONS[name]}에 해당해 조례 이격거리가 적용되지 않음",
                "ordinance": None,
                "statute": STATUTE,
            }

    # R2. 사람이 확정한 조례 데이터가 없으면 판정하지 않는다
    matched = setback_table[
        (setback_table["sgg_cd"].astype(str) == str(sgg_cd))
        & (setback_table["verified_by"].astype(str).str.strip() != "")
    ]
    if project_type:
        by_type = matched[matched["project_type"].isin([project_type, "all"])]
        if not by_type.empty:
            matched = by_type
    if matched.empty:
        return {
            "status": "판정불가",
            "rule": "R2",
            "reason": "해당 시군구의 조례 이격거리 규정이 아직 확인되지 않음 - 사람 확인 필요",
            "ordinance": None,
            "statute": STATUTE,
        }

    row = matched.iloc[0]
    ordinance = _ordinance_payload(row)

    # R3. 확인 결과 조례에 이격거리 규정 자체가 없는 경우
    if ordinance["distance_m"] is None:
        return {
            "status": "비저촉",
            "rule": "R3",
            "reason": f"{ordinance['name']}에 이격거리 규정 없음 ({str(row['verified_at']).strip()} 확인)",
            "ordinance": ordinance,
            "statute": STATUTE,
        }

    # R4. 법 시행 전 신청분은 종전 조례가 그대로 적용된다 (부칙 제3조 적용례)
    if apply_date < STATUTE_EFFECTIVE_DATE:
        return {
            "status": "조건부",
            "rule": "R4",
            "reason": (
                f"법 제27조의3 시행({STATUTE_EFFECTIVE_DATE.isoformat()}) 전 신청분이므로 "
                f"종전 조례가 그대로 적용됨 - 실제 저촉 여부는 현장 확인 필요"
            ),
            "ordinance": ordinance,
            "statute": STATUTE,
        }

    # R6. 보호구역 해당 여부를 모르면 R5 판단 자체가 불가능하다
    if zone_flags is None:
        return {
            "status": "판정불가",
            "rule": "R6",
            "reason": "역사문화환경보존지역·생태경관보전지역 해당 여부를 확인할 수 없어 판단 불가 - 사람 확인 필요",
            "ordinance": ordinance,
            "statute": STATUTE,
        }

    # R5-a. 법 제1항 각 호에 해당하면 지자체가 이격거리를 적용할 수 있다
    for flag, basis in PROTECTED_ZONE_FLAGS.items():
        if zone_flags.get(flag):
            return {
                "status": "조건부",
                "rule": "R5-a",
                "reason": f"법 {basis}에 해당해 조례 이격거리를 적용할 수 있음 - 실제 저촉 여부는 현장 확인 필요",
                "ordinance": ordinance,
                "statute": STATUTE,
            }

    # R5-b. 원칙적으로 적용 불가지만, 예외를 정할 대통령령이 아직 없다
    return {
        "status": "판정불가",
        "rule": "R5-b",
        "reason": (
            "법 제27조의3제1항에 따라 원칙적으로 이격거리를 적용할 수 없으나, "
            "같은 조 제1항제3호·제2항이 위임한 대통령령이 확인되지 않아 예외 해당 여부를 "
            "확정할 수 없음 - 사람 확인 필요"
        ),
        "ordinance": ordinance,
        "statute": STATUTE,
    }


def to_judgment_row(result):
    """check_setback 결과를 기존 판정표 행 구조로 변환한다."""
    ordinance = result.get("ordinance")
    if ordinance:
        excerpt = f"[{ordinance['name']} {ordinance['article']}] {ordinance['excerpt']}"
        source_url = ordinance["source_url"] or None
    else:
        excerpt = None
        source_url = None

    statute = result["statute"]
    law_excerpt = f"{excerpt}\n\n[{statute['name']} {statute['article']}] 시행 {statute['effective_date']}" \
        if excerpt else f"[{statute['name']} {statute['article']}] 시행 {statute['effective_date']}"

    return {
        "raw_text": "이격거리(조례)",
        "rule_id": f"SETBACK-{result['rule']}",
        "status": result["status"],
        "law_excerpt": law_excerpt,
        "source_url": source_url,
        "note": result["reason"],
    }
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `python -m pytest tests/test_setback_check.py -v`
Expected: PASS (11개)

- [ ] **Step 5: 전체 테스트 + 커밋**

Run: `python -m pytest -q`

```bash
git add src/setback_check.py tests/test_setback_check.py
git commit -m "feat(setback): 이격거리 규정 판정 엔진 추가

법 제27조의3의 적용 구조(원칙 금지 + 예외 + 배제)를 규칙 R1~R6으로 구현.
시행령에 이격거리 조문이 없어 예외 범위를 확정할 수 없는 구간(R5-b)은
비저촉/저촉으로 단정하지 않고 판정불가로 둔다.
반경 내 주택 실측이 범위 밖이므로 이 엔진은 '저촉'을 반환하지 않는다."
```

---

### Task 5: 파이프라인 연결 (sgg_cd 전달 + CLI 통합)

판정 엔진에는 시군구코드가 필요한데 현재 파이프라인은 `sgg_nm`만 넘긴다. 이를 뚫고, CLI 판정 단계에 이격거리 행을 추가한다.

**Files:**
- Modify: `src/query_site_data.py` (`resolve_zone_info` 반환값)
- Modify: `tests/test_query_site_data.py`
- Modify: `src/judge.py` (`__main__` 블록)

**Interfaces:**
- Consumes: Task 4의 `check_setback`, `to_judgment_row`
- Produces: `resolve_zone_info` 반환 dict에 `"sgg_cd"` 키 추가 (GIS 경로일 때만 존재)

- [ ] **Step 1: 실패하는 테스트 작성**

`tests/test_query_site_data.py`에 추가한다.

```python
def test_gis_fallback_passes_sgg_cd_for_setback_check():
    eum_result = {"status": "no_data", "reason": "빈 응답"}
    vworld_result = {"status": "ok", "lon": POINT_INSIDE["lon"], "lat": POINT_INSIDE["lat"]}

    result = resolve_zone_info(vworld_result, eum_result, geojson_path=ZONING_FIXTURE)

    assert result["sgg_cd"] == "12110"
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `python -m pytest tests/test_query_site_data.py::test_gis_fallback_passes_sgg_cd_for_setback_check -v`
Expected: FAIL — `KeyError: 'sgg_cd'`

- [ ] **Step 3: 최소 구현**

`src/query_site_data.py`의 `resolve_zone_info` 반환 dict에 한 줄을 추가한다(`"sgg_nm"` 바로 아래).

```python
        "sgg_nm": zone_result["sgg_nm"],
        "sgg_cd": zone_result["sgg_cd"],
        "zone_geometry": zone_result["geometry"],
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `python -m pytest tests/test_query_site_data.py -v`
Expected: PASS

- [ ] **Step 5: CLI 판정 단계에 이격거리 행 추가**

`src/judge.py`의 `__main__` 블록에서 `result = judge(...)` 다음 줄에 삽입한다.

```python
    result = judge(matching_result, rule_table, observed_values)

    # FR-10. 이격거리 규정 판정을 같은 판정표에 한 행으로 덧붙인다.
    setback_path = BASE_DIR / "data" / "setback_table.csv"
    sgg_cd = raw_query_result.get("eum", {}).get("sgg_cd") if raw_query_path.exists() else None
    if setback_path.exists() and sgg_cd:
        from datetime import date
        from setback_check import check_setback, to_judgment_row

        setback_table = pd.read_csv(setback_path, dtype=str).fillna("")
        setback_result = check_setback(
            sgg_cd=sgg_cd,
            project_type=raw_query_result.get("input", {}).get("project_type", ""),
            apply_date=date.today(),
            exemptions=[],       # CLI에서는 면제사유를 입력받지 않는다 (웹앱에서만 지원)
            zone_flags=None,     # 보호구역 판별은 아직 자동화되지 않았다 -> R6(판정불가)
            setback_table=setback_table,
        )
        result.append(to_judgment_row(setback_result))
```

> `zone_flags=None`을 넘기므로 CLI에서는 대부분 R6(판정불가)가 나온다. 의도된 동작이다 — 보호구역 자동 판별이 아직 없으므로 사람이 확인해야 한다.

- [ ] **Step 6: CLI 동작 확인**

Run:
```bash
python src/query_site_data.py "목포시 옥암동 1" 태양광 990
python src/match_regulations.py
python src/judge.py
```
Expected: `data/judgment_result.json` 마지막 항목이 `"raw_text": "이격거리(조례)"` 인 행. `setback_table.csv`가 헤더만 있으므로 `rule_id`는 `SETBACK-R2`.

- [ ] **Step 7: 전체 테스트 + 커밋**

Run: `python -m pytest -q`

```bash
git add src/query_site_data.py src/judge.py tests/test_query_site_data.py
git commit -m "feat(setback): 판정 파이프라인에 이격거리 항목 연결

GIS 조회 결과의 시군구코드를 파이프라인 밖으로 전달하고,
판정 단계에서 이격거리 판정 행을 판정표에 덧붙인다.
CLI는 보호구역 여부를 아직 자동 판별하지 못하므로 판정불가(R6)로 넘어간다."
```

---

### Task 6: 웹앱 연결 (입력 확장 + 판정표 노출)

면제사유는 사용자만 알 수 있는 정보라 입력을 받아야 한다. 허가신청 예정일도 받아 R4/R5 분기를 정확히 태운다.

**Files:**
- Modify: `api/site_judge.py`
- Modify: `public/index.html`

**Interfaces:**
- Consumes: Task 4의 `check_setback`/`to_judgment_row`, Task 5의 `sgg_cd`
- Produces: `/api/site_judge` 응답의 `table` 배열 마지막에 이격거리 행. 새 쿼리 파라미터 `exemptions`(쉼표구분), `apply_date`(YYYY-MM-DD)

- [ ] **Step 1: API에 이격거리 판정 추가**

`api/site_judge.py`의 import 블록(try 안)에 두 줄을 추가한다.

```python
    from setback_check import check_setback, to_judgment_row  # noqa: E402
```

`run_pipeline` 시그니처와 본문을 바꾼다.

```python
def run_pipeline(address: str, project_type: str, capacity_kw: float, vworld_result: dict = None,
                  exemptions: list = None, apply_date_str: str = "") -> dict:
    raw = query_site(address, project_type, capacity_kw, vworld_result=vworld_result)
    eum_result = raw.get("eum") or {}

    response = {
        "input": raw["input"],
        "vworld": raw["vworld"],
        "eum": eum_result,
        "table": [],
        # 지도 위성 타일 표시용(브이월드 WMTS) - 이 키로는 지도 타일만 그리고,
        # CORS로 막혀있는 지오코더 API는 여전히 서버(이 함수)에서만 호출한다.
        "vworld_map_key": os.environ.get("VWORLD_API_KEY", ""),
    }

    if eum_result.get("status") != "ok":
        response["note"] = "용도지역 등 조회 실패 - " + eum_result.get("reason", "사유 불명")
        return response

    rule_table = pd.read_csv(RULE_TABLE_PATH, dtype=str).fillna("")
    matching = match_regulations(eum_result, rule_table)
    judged = judge(matching, rule_table, {"capacity_kw": capacity_kw})

    setback_row = _judge_setback(eum_result, project_type, exemptions, apply_date_str)
    if setback_row is not None:
        judged.append(setback_row)

    df = generate_output_table(judged)
    response["table"] = df.to_dict(orient="records")
    return response
```

`run_pipeline` 바로 위에 헬퍼를 추가한다.

```python
SETBACK_TABLE_PATH = os.path.join(DATA_DIR, "setback_table.csv")


def _judge_setback(eum_result: dict, project_type: str, exemptions: list, apply_date_str: str):
    """이격거리 판정 행을 만든다. 시군구코드를 모르면(전남 밖 등) 행을 만들지 않는다."""
    sgg_cd = eum_result.get("sgg_cd")
    if not sgg_cd or not os.path.isfile(SETBACK_TABLE_PATH):
        return None

    try:
        apply_date = date.fromisoformat(apply_date_str) if apply_date_str else date.today()
    except ValueError:
        apply_date = date.today()

    setback_table = pd.read_csv(SETBACK_TABLE_PATH, dtype=str).fillna("")
    result = check_setback(
        sgg_cd=sgg_cd,
        project_type=project_type,
        apply_date=apply_date,
        exemptions=exemptions or [],
        zone_flags=None,   # 보호구역 자동 판별은 아직 없다 -> R6(판정불가)
        setback_table=setback_table,
    )
    return to_judgment_row(result)
```

파일 상단 import에 `date`를 추가한다.

```python
from datetime import date
```

`do_GET`에서 파라미터를 읽어 넘긴다. `capacity_kw` 파싱 바로 아래에 추가한다.

```python
        exemptions = [s for s in (query.get("exemptions") or [""])[0].split(",") if s]
        apply_date_str = (query.get("apply_date") or [""])[0].strip()
```

`run_pipeline` 호출부를 바꾼다.

```python
            result = run_pipeline(address, project_type, capacity_kw,
                                   vworld_result=vworld_result,
                                   exemptions=exemptions, apply_date_str=apply_date_str)
```

- [ ] **Step 2: 로컬에서 API 함수 직접 검증**

Run:
```bash
python -c "
import importlib.util, os, json
spec = importlib.util.spec_from_file_location('api.site_judge', os.path.join('api','site_judge.py'))
m = importlib.util.module_from_spec(spec); spec.loader.exec_module(m)
print('IMPORT_ERROR:', m.IMPORT_ERROR)
v = {'status':'ok','type':'client_jsonp','lon':126.44610514616278,'lat':34.80924646756152,'refined_addr':'테스트'}
r = m.run_pipeline('목포시 옥암동 1','태양광',990,vworld_result=v,exemptions=['지붕형'])
print(json.dumps(r['table'], ensure_ascii=False, indent=2))
"
```
Expected: `IMPORT_ERROR: None`, 그리고 `table` 마지막 행이 `항목: 이격거리(조례)`, `판정: 비저촉`(지붕형이므로 R1).

- [ ] **Step 3: 프론트엔드 입력 추가**

`public/index.html`의 용량 입력 `<div class="row">` 블록 **바로 아래**, `<button type="submit" ...>` **위**에 삽입한다.

```html
      <label>해당하는 경우 선택 (법 제27조의3제3항 — 이격거리 적용 배제)</label>
      <div class="row" style="gap:16px;flex-wrap:wrap">
        <label style="flex:none;color:var(--ink);margin:0">
          <input type="checkbox" class="exemption" value="주민참여형" style="width:auto"> 주민참여형
        </label>
        <label style="flex:none;color:var(--ink);margin:0">
          <input type="checkbox" class="exemption" value="지붕형" style="width:auto"> 지붕형
        </label>
        <label style="flex:none;color:var(--ink);margin:0">
          <input type="checkbox" class="exemption" value="자가소비용" style="width:auto"> 자가소비용
        </label>
      </div>
      <label for="apply_date">개발행위허가 신청 예정일</label>
      <input id="apply_date" type="date">
```

- [ ] **Step 4: 프론트엔드 요청에 값 싣기**

`public/index.html`의 submit 핸들러에서 `capacityKw`를 읽는 줄 아래에 추가한다.

```javascript
    const exemptions = Array.from(document.querySelectorAll(".exemption:checked"))
      .map(function (el) { return el.value; }).join(",");
    const applyDate = document.getElementById("apply_date").value;
```

URL 조립부에 두 파라미터를 덧붙인다.

```javascript
      const url = "/api/site_judge?address=" + encodeURIComponent(address) +
        "&project_type=" + encodeURIComponent(projectType) +
        "&capacity_kw=" + encodeURIComponent(capacityKw) +
        "&lon=" + geo.lon + "&lat=" + geo.lat +
        "&refined_addr=" + encodeURIComponent(geo.refined_addr) +
        "&exemptions=" + encodeURIComponent(exemptions) +
        "&apply_date=" + encodeURIComponent(applyDate);
```

- [ ] **Step 5: JS 문법 검증**

Run:
```bash
python -c "
import re
html = open('public/index.html', encoding='utf-8').read()
open('/tmp/x.js','w',encoding='utf-8').write(re.findall(r'<script>(.*?)</script>', html, re.S)[0])
"
node --check /tmp/x.js && echo "JS SYNTAX OK"
```
Expected: `JS SYNTAX OK`

- [ ] **Step 6: 전체 테스트 + 커밋**

Run: `python -m pytest -q`

```bash
git add api/site_judge.py public/index.html
git commit -m "feat(webapp): 이격거리 판정을 판정표에 노출

면제사유(주민참여형·지붕형·자가소비용)와 개발행위허가 신청 예정일을
입력받아 이격거리 판정 결과를 판정표에 한 행으로 추가한다.
보호구역 자동 판별이 없어 대부분 판정불가로 나가며, 이는 의도된 안전 동작이다."
```

---

### Task 7: 문서 갱신

**Files:**
- Modify: `PRD.md`
- Modify: `HANDOVER.md`

**Interfaces:**
- Consumes: Task 1~6의 결과
- Produces: 없음

- [ ] **Step 1: PRD.md에 FR-10 추가**

`## 6-4. FR-9. 인수인계 메모` 절 **바로 아래**에 추가한다.

```markdown
## 6-5. FR-10. 이격거리 규정 판정 (✅ 구현 완료 — [src/setback_check.py](src/setback_check.py))

「신에너지 및 재생에너지 개발ㆍ이용ㆍ보급 촉진법」 제27조의3(2026-09-18 시행)에 따라 지자체 조례의 이격거리 적용이 원칙적으로 금지된다. 부지의 시군구 조례가 실제로 적용되는지를 판정해 판정표에 한 행으로 추가한다.

- 조례 수치는 사람이 확정한 `data/setback_table.csv`에서 읽는다. `verified_by`가 빈 행은 판정에 쓰지 않는다
- 판정 규칙 R1~R6은 [설계서](docs/20260916_설계_FR-10%20이격거리%20규정%20판정%20v1.0.md) §6-2 참조
- **시행령에 이격거리 조문이 없어** 예외 범위를 확정할 수 없는 구간(R5-b)은 판정불가로 둔다
- 반경 내 주택 실측은 범위 밖이므로 이 기능은 `저촉`을 반환하지 않는다
```

- [ ] **Step 2: HANDOVER.md에 운영 절차 추가**

`## 5. 알려진 한계` 절 **바로 위**에 추가한다.

```markdown
## 5-6. 이격거리 규정 판정 운영 (FR-10, 2026-09-17)

**데이터 갱신 절차**
1. `python scripts/collect_setback_draft.py` — 전남 22개 시군구 조례에서 이격거리 조문 초안 수집 → `data/setback_table_draft.csv`
2. 초안의 조문을 읽고 `distance_m`, `min_house_count`, `target`, `gosi_date`를 확정
3. 확정한 행을 `data/setback_table.csv`로 옮기고 `verified_by`, `verified_at`을 채운다
4. **`verified_by`가 비어 있으면 판정에 사용되지 않는다** — 미완성 상태로 배포해도 잘못된 판정이 나가지 않는다

**판정 규칙 요약** (상세: docs/20260916_설계_FR-10 …)

| 규칙 | 조건 | 결과 |
|---|---|---|
| R1 | 주민참여형·지붕형·자가소비용 | 비저촉 (법 제27조의3제3항) |
| R2 | 조례 미확인 | 판정불가 |
| R3 | 조례에 이격거리 규정 없음(확인 완료) | 비저촉 |
| R4 | 허가신청일 < 2026-09-18 | 조건부 (종전 조례 적용) |
| R5-a | 시행 후 + 역사문화환경보존지역·생태경관보전지역 | 조건부 |
| R5-b | 시행 후 + 그 외 | 판정불가 (시행령 미비) |
| R6 | 보호구역 여부 미확인 | 판정불가 |

**알아둘 점**
- 시행일 상수(`STATUTE_EFFECTIVE_DATE`)는 부칙 "공포 후 6개월 경과일"에 근거한 사람 확정값이다. 법령 API가 `efYd`를 무시해 실증하지 못했다
- 보호구역(역사문화환경보존지역·생태경관보전지역) 자동 판별은 아직 없다. 그래서 시행 후 신청분은 대부분 R5-b/R6으로 판정불가가 된다 — 정상 동작이며, 사람이 확인해야 한다는 뜻이다
```

- [ ] **Step 3: 커밋**

```bash
git add PRD.md HANDOVER.md
git commit -m "docs: FR-10 이격거리 규정 판정 반영

PRD에 기능 명세를, HANDOVER에 조례 데이터 갱신 절차와
판정 규칙 요약표를 추가."
```

---

## Self-Review

**1. 스펙 커버리지**

| 스펙 항목 | 구현 태스크 |
|---|---|
| §3-1 조례 수치 표 확보 | Task 3 |
| §3-1 판정 엔진 | Task 4 |
| §3-1 판정표 1행 추가 | Task 5(CLI), Task 6(웹앱) |
| §3-1 law_lookup 가지번호 수정 | Task 1 |
| §5 CSV 스키마 | Task 3 Step 1 |
| §6-2 규칙 R1~R6 | Task 4 Step 3 |
| §6-3 반환 구조 | Task 4 Step 3 |
| §7 가지번호 버그 원인·수정 | Task 1 |
| §7-4 기존 규칙표 영향 재확인 | Task 2 |
| §8 웹앱 입력 확장 | Task 6 Step 3~4 |
| §9 테스트 10종 | Task 4 Step 1 (11개 작성) |
| §10 데이터 수집 절차 | Task 3, Task 7 Step 2 |

누락 없음.

**2. 플레이스홀더 점검** — "TBD/TODO/적절히 처리" 없음. 모든 코드 단계에 실제 코드 포함. Task 2는 조건 분기(의심 행 없으면 종료)를 명시해 빈 작업을 방지.

**3. 타입 일관성**

- `check_setback(...) -> dict` 반환 키: `status`, `rule`, `reason`, `ordinance`, `statute` — Task 4 정의, Task 5·6에서 `to_judgment_row`로만 소비하므로 키 불일치 위험 없음
- `to_judgment_row(...) -> dict` 반환 키: `raw_text`, `rule_id`, `status`, `law_excerpt`, `source_url`, `note` — `generate_output_table`이 읽는 키(`raw_text`, `status`, `law_excerpt`, `source_url`)를 모두 포함
- `parse_article(xml, article_no, branch_no=None)` — Task 1에서 기본값을 둬 기존 호출부(`parse_article(FIXTURE, "76")`) 무변경
- `resolve_zone_info` 반환의 `sgg_cd` — Task 5에서 생산, Task 6의 `_judge_setback`에서 `eum_result.get("sgg_cd")`로 소비. 키 이름 일치

---

## Execution Handoff

계획 완료. 실행 방식 두 가지 중 선택:

1. **Subagent-Driven (권장)** — 태스크마다 새 서브에이전트를 붙이고 태스크 사이에 검토. 컨텍스트가 깨끗하게 유지되고 반복이 빠름
2. **Inline Execution** — 이 세션에서 순차 실행, 체크포인트마다 검토
