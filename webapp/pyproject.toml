[project]
name = "webapp"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "requests",
    "shapely",
    "pandas",
    "beautifulsoup4",
    "lxml",
    "python-dotenv",
]

[tool.vercel]
entrypoint = "api.site_judge:handler"
