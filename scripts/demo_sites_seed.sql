-- 시연용 부지 3건 등록 (Task 7)
--
-- 실행 방법: Supabase 대시보드 > SQL Editor 에 통째로 붙여넣고 Run.
-- 전제: scripts/supabase_schema_parcels.sql 이 먼저 실행되어 demo_sites 테이블이
-- 존재해야 한다.
--
-- ★★★ 아래 lon/lat 은 자리표시자(placeholder)다 — 실행 전 반드시 Task 4에서
-- parcels 테이블에 실제로 적재된 필지의 좌표로 바꿔야 한다. ★★★
-- 이 파일을 만든 세션에는 Supabase 접속 정보가 없어 parcels 테이블을 조회할 수
-- 없었으므로, 좌표를 확인·대체하지 않았다. 자리표시자 그대로 넣으면 parcel_at()이
-- 해당 좌표 반경 50m 안에서 아무 필지도 못 찾아 필지 탭이 "미제공"으로 뜬다 —
-- 이 과제가 막으려는 바로 그 시연 사고다. 실제 좌표는 아래 방법으로 확인한다.
--   select pnu, 소재지, 지번, st_x(geom) as lon, st_y(geom) as lat
--   from parcels where 소재지 like '%신안군 안좌면 마명리%';  -- (다른 두 건도 동일하게)
--
-- 등기부·조례로 사전 검증된 부지를 고른 이유는 HANDOVER.md/PRD.md의 시연 시나리오를
-- 참고했다(등기부로 면적 100% 확보, 용도지역 경계 부근 판정불가 사례 등) — 이 파일은
-- 그 근거까지 다시 쓰지 않는다.

insert into demo_sites (display_ord, site_name, address, lon, lat, project_type, capacity_kw, note)
values
 (1, '신안군 안좌면 마명리 (12필지)', '전남 신안군 안좌면 마명리 152',
     126.123456, 34.812345, '태양광', 3000, '등기부로 면적 100% 확보된 검증 부지'),
 (2, '영광군 염산면 봉남리', '전남 영광군 염산면 봉남리 1',
     126.400000, 35.250000, '태양광', 2000, '등기부 109건으로 면적 완성한 부지'),
 (3, '목포시 인근 (용도지역 경계)', '전남 목포시 옥암동 1',
     126.400000, 34.800000, '태양광', 1000, '경계 부근이라 판정불가가 나오는 사례');
